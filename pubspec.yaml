name: caesar_zipher
description: "Программа для работы с принтером VideoJet по протоколу Zipher."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.11.1

dependencies:
  ctelnet: ^0.3.1
  editable: ^2.0.0
  file_picker: ^10.3.10
  flutter:
    sdk: flutter
  fluttertoast: ^9.0.0
  logger: ^2.7.0
  path_provider: ^2.1.5
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
